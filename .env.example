NODE_ENV=production
PORT=3001

# Optional: only needed if you want auto-uptime to ping a custom URL
UPTIME_URL=

# Optional: MongoDB connection string (only if database.type = "mongodb" in config.json)
MONGODB_URI=
